ThemeParkList.Views.ParksIndexItem = Backbone.View.extend({
  template: JST['parks/parks_index_item'],
  className: "parks-index-item",

  initialize: function () {
  },

  render: function () {
    var i = this.model.escape('image_url');
    this.$el.html(this.template({ park: this.model }));
    return this;
  }
});